version: v1.0
name: Promotions and Auto-promotions
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Deploy
    task:
      jobs:
        - name: Staging
          commands:
            - checkout
            - nvm use 10
            - cache restore website-build
            - ls -la
            - '# cat deploy/deploy-script.js'
            - '# npm run deploy'
            - echo "deploy successfull"
      secrets:
        - name: digipop ftp
      prologue:
        commands:
          - echo "evo liste pre svega"
          - ls -la
          - echo "============================="
