version: v1.0
name: Promotions and Auto-promotions
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Deploy
    task:
      jobs:
        - name: Staging
          commands:
            - pwd
            - checkout
            - nvm use 10
            - cache restore website-build
            - npm i
            - echo "evo liste POSLE svega"
            - pwd
            - ls -la
            - cd ..
            - cp ~/deploy/* ~/digipop/
            - cd digipop
            - ls -l
            - echo "============================="
            - node deploy-script.js
            - echo "deploy successfull"
      secrets:
        - name: digipop ftp
      prologue:
        commands:
          - echo "evo liste pre svega"
          - ls -la
          - echo "============================="
